# .env.dev - Environment variables for LOCAL Docker development

# --- Django Settings ---
# Use a simple, non-secret key for local development.
SECRET_KEY=django-insecure-a-local-dev-secret-key-for-ai4s-project

# Set DEBUG to 1 (which means True) for local development.
DEBUG=1

# Define the allowed hostnames inside the Docker network.
ALLOWED_HOSTS=localhost,127.0.0.1,nginx

# --- PostgreSQL Container Settings ---
# These are the credentials the PostgreSQL container will use to initialize itself.
# These values MUST be present.
SQL_DATABASE=ai4s_db
SQL_USER=ai4s_user
SQL_PASSWORD=a_strong_password_for_db

# --- Database URL for Django ---
# This is the full connection string that Django will use.
# The credentials here MUST MATCH the SQL_ variables above.
# The hostname '@db' refers to the 'db' service in your docker-compose.yml.
DATABASE_URL=postgres://ai4s_user:a_strong_password_for_db@db:5432/ai4s_db